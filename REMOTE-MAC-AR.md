# تشغيل macOS وXcode عن بُعد من لابتوب Windows

هذا المستودع يحتوي على مهمة GitHub Actions يدوية باسم **Remote macOS + Xcode**. عند تشغيلها، ينشئ GitHub جهاز macOS مؤقتًا، يفتح مشروع Anime Shelf في Xcode، يشغّل iPhone Simulator، ثم يعرض سطح المكتب داخل متصفح اللابتوب.

## الفكرة

```text
لابتوب Windows
    ↓ اتصال HTTPS مؤقت ومحمي بكلمة مرور
صفحة noVNC داخل المتصفح
    ↓
خدمة Screen Sharing في macOS
    ↓
Xcode + AnimeShelf.xcodeproj + iPhone Simulator
```

لا يتم تشغيل macOS تلقائيًا عند كل تعديل. المهمة تعمل فقط عند تشغيلها يدويًا من صفحة Actions، ولا يبقى الجهاز بعد انتهاء المهمة.

## طريقة التشغيل

1. افتح المستودع في GitHub.
2. افتح تبويب **Actions**.
3. اختر **Remote macOS + Xcode** من القائمة.
4. اضغط **Run workflow**.
5. اختر مدة الجلسة، ثم اضغط الزر الأخضر **Run workflow**.
6. افتح التشغيل الجديد، ثم انتظر اكتمال خطوة **Prepare macOS, Xcode, and remote desktop**.
7. في أعلى صفحة التشغيل افتح **Summary**، واضغط رابط **افتح Xcode وmacOS من المتصفح**.
8. عند ظهور نافذة بيانات الدخول، اكتب اسم المستخدم `remotemac` وكلمة مرور سطح المكتب التي أُعدّت لك، ثم اضغط **Send credentials**.

بعد الاتصال ستظهر شاشة macOS، ويفتح Xcode والمشروع والمحاكي تلقائيًا. تستطيع استخدام الماوس ولوحة المفاتيح كأن الجهاز أمامك.

## حفظ التعديلات

الجهاز مؤقت، لذلك أي تعديل غير مرفوع إلى GitHub سيختفي عند انتهاء المهمة. قبل الخروج:

1. من Xcode اختر **Integrate > Commit** أو **Source Control > Commit** حسب إصدار Xcode.
2. حدّد الملفات واكتب وصفًا للتعديل ثم نفّذ Commit.
3. نفّذ Push إلى فرع `main`.
4. تأكد من ظهور التعديل في GitHub.

يمكن أيضًا فتح Terminal داخل macOS وتنفيذ:

```bash
git status
git add .
git commit -m "Save changes from remote Mac"
git push origin main
```

## إنهاء الجلسة

عند الانتهاء ارجع إلى تشغيل GitHub Actions واضغط **Cancel workflow**. لا تترك المهمة تعمل بلا حاجة لأن مستودع Anime Shelf خاص، ووقت macOS في المستودعات الخاصة يُخصم من رصيد Actions أو يُحاسب بالدقيقة بعد نفاد الرصيد.

أقصى مدة وضعتها المهمة هي 340 دقيقة لإتاحة وقت كافٍ للإعداد والإغلاق قبل حد GitHub البالغ 6 ساعات لكل Job.

## الأمان

- عنوان الاتصال عشوائي ومؤقت، ويتوقف عند انتهاء المهمة.
- الاتصال من اللابتوب إلى العنوان يستخدم HTTPS.
- لا تُكتب كلمة المرور داخل ملفات المستودع؛ هي محفوظة في GitHub Actions Secrets.
- المستخدم `remotemac` يُنشأ داخل جهاز الجلسة فقط ويُحذف مع الجهاز عند انتهاء المهمة.
- لا ترسل رابط الجلسة أو كلمة المرور لأي شخص.
- المهمة لا تفتح منفذ VNC مباشرة على الإنترنت؛ المتصفح يصل إليه من خلال جسر HTTPS مؤقت.

## إذا لم تظهر الشاشة

- تأكد أن خطوة الإعداد انتهت بعلامة صح.
- أعد تحميل رابط سطح المكتب وانتظر دقيقة بعد فتحه أول مرة.
- إذا ظهرت شاشة noVNC فقط، اضغط **Connect** وأدخل كلمة المرور.
- إذا توقفت المهمة، شغّل جلسة جديدة؛ كل تشغيل ينشئ macOS جديدًا بعنوان جديد.
